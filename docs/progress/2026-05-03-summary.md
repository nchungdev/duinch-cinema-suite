# Progress Report - May 3, 2026

## Overview
Successfully unified the UI design language across the platform and improved backend matching logic for TV shows.

## Key Changes

### UI Unification
- **TVGallery.tsx**: Redesigned server selection nodes to match the "Discovery Engine" style.
  - Implemented 10x10 rounded-2xl icons for better visual consistency.
  - Replaced text-based audio labels with solid, color-coded badges (Green for Vietsub, Pink for Lồng Tiếng).
  - Added provider tags (KKPHIM, OPHIM, etc.) and information density improvements.
- **MediaStreamer.tsx**: Updated the player's server switcher popup to use the same unified design language.
- **Visual Excellence**: Standardized active states with premium blue glows and consistent padding/spacing across all row-based components.

### Backend Improvements
- **phimapi_base.py**:
  - Improved TV show matching by allowing a wider year gap (10 years) for series starts.
  - Added logic to match against specific season years from TMDB metadata.
  - Implemented `safe_int` utility to handle inconsistent API data (e.g., years or totals provided as strings).
- **discovery.py**:
  - Enhanced identity leakage checks by verifying against all alternative titles from TMDB, reducing false positives in search results.

### Maintenance
- **Cleanup**: Removed 6 redundant `.claude/skills` files to reduce context bloat and clean up the workspace.
- **Optimization**: Cleaned up debug `console.log` statements in `TorrentRow.tsx`.

## Next Steps
- [x] Update API configuration to point to the production domain (`https://omv-jdownloader-dashboard.onrender.com/`).
  - **Dynamic Config**: Implemented `/api` relative pathing with Vite proxy support.
  - **Dev**: Proxies `http://localhost:5173/api` -> Render.
  - **Prod**: Uses `https://omv-jdownloader-dashboard.onrender.com/api` automatically.
- [ ] Monitor JDownloader connectivity with real credentials.
- [ ] Further refine Discovery Engine navigation filters.
